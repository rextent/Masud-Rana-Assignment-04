Here are the answer of the required Questions:

1. The main difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll is: by getElementById we can only call and get an element by ID from HTML to Javascript and we can get only one specific element, by getElementsByClassName we can only call or get element by CSS selector and also we can get multiple elements by using this getElementsByClassName, and by querySelector we can get only the first elements if multiple CSS class called, and also by querySelectorAll we can get all mathing element and also get nodelist.

2. to create and insert a element into the DOM- at first we will create an element by using createElement('html tag'), then we have to add the content and class, and after that we should insert it to dom by using appendChild


3. Event Bubbling is a process, Wheere, if an event occur an child it's goes step by step to their parent. such as button>div>body>document


4. Event Delegation means, We can give eventListener  to a parent except to give their child. To reduece the code length, It's very usefull.


5. preventDefault() are stopping the default behaviours of a element and stopPropafation() are stoping the event Bubbling, So event cant go to their parent.